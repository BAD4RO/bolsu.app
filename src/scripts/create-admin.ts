// Administrative provisioning must happen in a trusted, audited environment.
// The old hard-coded account creation script is intentionally disabled.
console.error('Provisionamento automático desativado. Gerencie administradores pelo ambiente autorizado.');
process.exitCode = 1;
export {};
